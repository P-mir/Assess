runtime: python
env: flex
entrypoint: gunicorn --timeout 18000 -k uvicorn.workers.UvicornWorker API:app 
# entrypoint: gunicorn -w 4 --timeout 18000 -k uvicorn.workers.UvicornWorker API:app 


runtime_config:
  python_version: 3.7

# This sample incurs costs to run on the App Engine flexible environment.
# The settings below are to reduce costs during testing and are not appropriate
# for production use. For more information, see:
# https://cloud.google.com/appengine/docs/flexible/python/configuring-your-app-with-app-yaml
# manual_scaling:
#   instances: 1

automatic_scaling:
  min_num_instances: 0
  max_num_instances: 2

resources:
  cpu: 1
  memory_gb: 4
  disk_size_gb: 10

# readiness_check:
#   app_start_timeout_sec: 300